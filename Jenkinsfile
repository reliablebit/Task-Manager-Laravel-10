pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        echo 'Code is checked out by Jenkins automatically!'
      }
    }
    stage('List Files') {
      steps {
        sh 'ls -la'
      }
    }
  }
}
